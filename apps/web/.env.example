# Origin of the API. Leave empty when the API is served under the same domain
# (Vercel rewrite or the dev proxy); set it to the API's URL otherwise, e.g.
# VITE_API_URL=https://arsheefna-api.up.railway.app
VITE_API_URL=
