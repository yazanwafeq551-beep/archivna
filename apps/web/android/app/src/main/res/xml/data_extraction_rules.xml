<?xml version="1.0" encoding="utf-8"?>
<!--
  The app stores a refresh token on the device. Android's automatic cloud
  backup would copy it off the phone, so nothing here is eligible for backup
  or for transfer to a new device: a session belongs to one device.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
